import { ApiState } from 'state/types'
import axios from 'axios'

export const getInfo =  async():Promise<ApiState> => {

    // const response = await axios(process.env.REACT_APP_API_URL)
    
    // return response.data
    return null;
  }